# macOS / editor
.DS_Store

# Build / release artifacts (kept locally, never tracked)
release/
*.zip

# Logs
*.log

# Node toolchain (only relevant if a build step is added later)
node_modules/

# IDE / editor configs
.cursor/
.vscode/

# Secrets / env
.env
.env.*
